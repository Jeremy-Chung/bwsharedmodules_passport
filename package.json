{
  "name": "passport",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "eslint ./ --cache --ignore-pattern .gitignore",
    "fix": "eslint ./ --cache --fix --ignore-pattern .gitignore",
    "quiet-lint": "eslint ./ --quiet --cache --ignore-pattern .gitignore",
    "precommit-msg": "echo 'Pre-commit checks...' && exit 0",
    "prepush-msg": "echo 'Pre-push checks...' && exit 0"
  },
  "author": "Norman Chen <normanchen@bananawhale.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Jeremy-Chung/bwsharedmodules_passport.git"
  },
  "pre-commit": [
    "precommit-msg",
    "lint"
  ],
  "pre-push": [
    "prepush-msg",
    "lint",
    "test"
  ],
  "dependencies": {
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "eslint": "^4.18.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.9.0",
    "pre-commit": "^1.2.2",
    "pre-push": "^0.1.1"
  }
}
